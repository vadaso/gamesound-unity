# Basic GameSound AudioSource Sample

After importing a sound from **Window > GameSound**, drag the generated `.gamesound.asset` onto a `GameSoundAudioSource` component or use **Create AudioSource** in the GameSound window.
